// The portal's client-logo chip. The implementation is shared with the CRM,
// which shows the same mark on deals and projects — see
// src/components/ClientLogo.jsx.
export { default, companyLogoSrc } from '../components/ClientLogo.jsx';
