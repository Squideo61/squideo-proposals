{
  "name": "squideo-extension",
  "version": "0.2.1",
  "private": true,
  "description": "Squideo CRM in-Gmail extension",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js",
    "package": "node build.js && node -e \"const {execSync}=require('child_process'),{readFileSync}=require('fs'),v=JSON.parse(readFileSync('package.json','utf8')).version;execSync('powershell Compress-Archive -Path dist\\\\* -DestinationPath squideo-crm-'+v+'.zip -Force');console.log('Packaged squideo-crm-'+v+'.zip');\""
  },
  "dependencies": {
    "@inboxsdk/core": "^2.2.15",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.4.3"
  }
}
